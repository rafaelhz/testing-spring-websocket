# How to test a Spring WebSocket 

This project demonstrates how to write a integration test using Stomp Client.

## Running
- Run `mvn test` to run the tests
- Run `mvn spring-boot:run` to run the app
